/target
/.usage-*
.claude/

# The hanzoai/openapi checkout the README tells you to point OPENAPI at. It is
# a clone with its own .git, and untracked it is one `git add -A` away from
# landing here as orphaned cargo.
.openapi/
